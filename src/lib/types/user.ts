export enum GENDER_ENUM {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum USER_STATUS {
  PENDING = "pending",
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended",
}
